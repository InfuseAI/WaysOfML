{
  "name": "with-tailwindcss",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "ghpages": "npm run build && npm run export && cp docs/.nojekyll out/ && cp docs/CNAME out/ && rm -rf docs && mv out docs"
  },
  "dependencies": {
    "csv-parse": "^4.16.0",
    "next": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.4",
    "postcss": "^8.1.10",
    "puppeteer": "^10.0.0",
    "tailwindcss": "^2.1.1"
  }
}
